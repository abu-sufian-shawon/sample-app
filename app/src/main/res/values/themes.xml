<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.FrutasYVerduras" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryVariant">@color/primary_dark</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/primary</item>
        <item name="colorSecondaryVariant">@color/primary_dark</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="SplashScreen" parent="Theme.FrutasYVerduras">
        <item name="android:windowBackground">@android:color/white</item>
        <item name="android:statusBarColor">@color/white</item>
        <item name="android:windowFullscreen">true</item>
    </style>

    <!-- Set parent theme as app theme. -->
    <style name="DialogTheme" parent="Theme.FrutasYVerduras">
        <item name="android:statusBarColor">@color/primary_dark</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">false</item>
        <item name="android:windowIsFloating">false</item>
        <item name="android:windowActionBarOverlay">false</item>
        <item name="android:windowAnimationStyle">@style/DialogFragmentAnimation</item>
    </style>

    <style name="DialogFragmentAnimation">
        <item name="android:windowEnterAnimation">@anim/dialog_fragment_in</item>
        <item name="android:windowExitAnimation">@anim/dialog_fragment_out</item>
    </style>

    <style name="TransitionAnimation" parent="Theme.FrutasYVerduras">
        <item name="android:activityOpenEnterAnimation">@anim/dialog_fragment_in</item>
        <item name="android:activityCloseExitAnimation">@anim/dialog_fragment_out</item>
    </style>

</resources>